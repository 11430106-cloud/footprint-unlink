# 研究測試模式：題庫草稿與部署

本文件供團隊先審核題目。**目前是草稿，請確認題意、相近難度、學校告知與同意要求後，再開啟正式收案。** 原網站八題仍是學習體驗，不納入前後測分數。一般首頁不呼叫研究後端。研究入口為網站首頁加 `?mode=research`，例如 `https://11430106-cloud.github.io/footprint-unlink/?mode=research`。

## A、B 題庫草稿與答案

完整題幹、選項與答案只存於後端的 `research/banks.ts`。以下列出每題目的及正確選項 ID。前端只會從後端取得沒有 `correct` 欄位的題幹及選項。A、B 題型與判斷結構一致，但人物、媒介及情境不同；正式測試前仍須由團隊試答並確認難度相近。

| 題庫 | 題目 | 測量目的 | 正確答案 | 計分 |
| --- | --- | --- | --- | --- |
| A | `a-identity` 社團海報與作品 | 公開姓名、學校及連結形成身分線索 | `same`：若作品也連到社團頁面，可能連到同一人 | 單選正確 1 分 |
| A | `a-location` 四週跑步路線 | 重複起訖點透露常去區域，不能推定門牌 | `anchor`：經常停留的區域或固定起點 | 單選正確 1 分 |
| A | `a-time` 三週打卡 | 週期活動時間而非每天行程 | `routine`：可能有固定活動 | 單選正確 1 分 |
| A | `a-link` 暱稱及頭像 | 跨平台可連結性與判斷界線 | `possible`：可能被連結，仍需核對 | 單選正確 1 分 |
| A | `a-protection` 路線、時間、帳號 | 新情境選擇可行防護 | `hide-route`、`delay`、`review-link` 為適當；`more-detail` 錯誤 | 至少選 2 項適當且不選錯誤項才達標 |
| B | `b-identity` 社團名單與匿名作品 | 公開姓名、班級及連結形成身分線索 | `linked`：作品頁可能連到名單中的人，仍需核對 | 單選正確 1 分 |
| B | `b-location` 四週照片地圖標記 | 重複標記透露常去區域，不能推定門牌 | `anchor`：常出現的區域或集合地點 | 單選正確 1 分 |
| B | `b-time` 三週社團行事曆 | 週期活動時間而非每天行程 | `routine`：可能有固定活動 | 單選正確 1 分 |
| B | `b-link` 頻道、論壇與作品網站 | 跨平台可連結性與判斷界線 | `possible`：可能被連結，仍需核對 | 單選正確 1 分 |
| B | `b-protection` 公開值班表與頻道連結 | 新情境選擇可行防護 | `limit-schedule`、`hide-meeting`、`unlink` 為適當；`live-checkin` 錯誤 | 至少選 2 項適當且不選錯誤項才達標 |

四道線索題每題 25 分，前後測各自為 0、25、50、75、100 分。只比較同一匿名編號的前、後測：平均差＝每人後測分數減前測分數後再平均，單位「百分點」。A→B 與 B→A 隨機指派；防護題獨立判定，不加進線索分數。前測不揭曉答案。版本目前 `A-draft-1`、`B-draft-1`；改題時更新版本，將舊版保留在 `bankVersions`，讓未完成者續答且舊資料可辨識。後端將每題當時計分結果及版本寫入資料庫。

## 指標口徑

- 同意並開始：成功建立匿名編號的人數。各階段完成及流失按同一編號的伺服器時間計算，沒有同意的人不建紀錄。
- 配對：同一編號完成前、後測；退出者計入對應流失，不進成效計算。完成整個流程須交完問卷。
- 防護判斷：配對者的後測防護題達標 / 配對者（目標 ≥80%）。
- 行動意願：問卷選至少一項具體行動者 / 配對者（目標 ≥70%）；`none` 互斥，不表示已改變行為。
- 操作理解：完整完成且自填「不需」或「少量」協助者 / 所有同意並開始者（目標 ≥80%）。
- 平均辨識提升目標 ≥20 百分點；分數分布、各類答對率及各題錯誤選項僅用配對者。易用性及問題類型以完成問卷者為分母；完整流程時間取完成者分鐘數中位數。
- 12 人都完成時，80% 至少 10 人、70% 至少 9 人；後台實際依資料分母計算。沒有資料顯示「尚無資料」，小樣本只作描述，不做推論。

## 方案及權限

網站仍由 GitHub Pages 靜態發布。研究 API 使用 Cloudflare Worker，資料存在綁定的 D1；只有 Worker 有資料庫權限，瀏覽器沒有資料庫金鑰。後端才有答案並計分。管理頁由 Worker 提供，入口為 `https://<worker>.<subdomain>.workers.dev/admin`；Cloudflare Access 驗證團隊登入，Worker 再驗 JWT 簽章、發行者、AUD 和允許的管理者電郵。即使 Access 路徑誤配，沒有有效 JWT 仍無法取得統計、CSV 或刪除資料。不要把管理密碼、Cloudflare API token 或金鑰放到 GitHub、GitHub Actions 公開輸出、網站程式或對話中。

Worker 僅接受設定的網站來源；研究資料用匿名編號和私有續傳憑證，資料庫只存憑證雜湊。各階段以資料庫欄位及條件更新鎖定，重送不新增參與者。瀏覽器在研究模式暫存未送出的答案及續傳憑證，重新整理會向後端核對階段。跨裝置或清除網站資料後無法找回同一匿名編號；不要為了找回而蒐集身分資訊。一般模式不寫研究 localStorage、不呼叫研究 API。

團隊沒有主動記錄 IP、精確位置或裝置識別資料；Cloudflare 作為傳輸及資料處理服務，仍可能按其服務政策處理網路請求。`observability` 在 Worker 範例中關閉；勿另外啟用含請求內容的日誌。D1 主資料依 `RETENTION_DAYS` 每日 UTC 03:00 清理，管理頁亦可按日期或全部刪除。D1 的 Time Travel 回復歷史由 Cloudflare 管理，Free 方案可回復至 7 天、Paid 至 30 天前；主資料刪除不代表回復歷史即刻消失。請在正式收案前按學校規定完成未成年人告知及同意。

## 本機驗證（假資料和正式資料分開）

1. `npm ci`、`npm test`、`npx tsc --noEmit`。
2. `npm run research:db:local`；這只初始化本機 D1，不接正式資料庫。
3. 一個終端執行 `npm run research:dev`，預設 API 在 `http://127.0.0.1:8788`。
4. 另一個終端在同一專案目錄設定環境變數 `NEXT_PUBLIC_RESEARCH_API_URL=http://127.0.0.1:8788`（PowerShell：`$env:NEXT_PUBLIC_RESEARCH_API_URL='http://127.0.0.1:8788'`），再執行 `npm run dev`；開啟 `http://localhost:3000/?mode=research`。本機管理頁因未設定正式 Access，應回應 403；管理登入／CSV／刪除的簽章與資料流程已由自動測試驗證。
5. 如需清除本機假資料，關閉服務後只清理 `research/.wrangler/state`；勿對正式 D1 使用本機指令。

## 正式部署順序（需由團隊帳號持有人完成）

1. 先審核題庫與告知內容，決定保存天數。準備 Cloudflare 帳號並啟用 Zero Trust；只用團隊指定的電郵作 Access Allow policy，不使用整個學校電郵網域。參考 [Access 保護 Worker](https://developers.cloudflare.com/workers/configuration/cloudflare-access/) 與 [路徑匹配](https://developers.cloudflare.com/cloudflare-one/access-controls/policies/app-paths/)。
2. 在本機登入 Cloudflare CLI：`npx wrangler login`。建立正式 D1：`npx wrangler d1 create footprint-research`，保存回傳的 `database_id`。切勿把登入憑證分享給我或提交。
3. 將 `research/wrangler.example.jsonc` 複製成 **已忽略 Git** 的 `research/wrangler.jsonc`，填入 D1 ID、Cloudflare Access `TEAM_DOMAIN`、`POLICY_AUD`、`ADMIN_EMAILS`（逗號分隔、僅核准管理者）及 `RETENTION_DAYS`。`ALLOWED_ORIGIN` 保持 `https://11430106-cloud.github.io`。帳號資料及權限只設在 Cloudflare 與本機未追蹤設定。
4. `npx wrangler d1 migrations apply footprint-research --config research/wrangler.jsonc --remote`。確認目標是正式 D1 後執行 `npx wrangler deploy --config research/wrangler.jsonc`。從部署輸出取得 Worker URL。
5. 在 Cloudflare Zero Trust → Access → Applications 建立 self-hosted 應用，保護 Worker 主機上的 **`/admin`** 及 **`/api/admin/*`** 路徑，使用同一個應用 AUD 與精確核准電郵。`/api/start`、`/api/session`、`/api/pre`、`/api/experience`、`/api/post`、`/api/finish` 必須仍可供受測者使用。核對無登入時兩個管理路徑都不能讀資料；Worker 本身也再次驗 JWT。
6. GitHub 儲存庫 Settings → Secrets and variables → Actions → **Variables** 設 `RESEARCH_API_URL` 為公開 Worker URL（無路徑），`RESEARCH_RETENTION_DAYS` 為相同保存天數。兩者都不是密碼；不要設定 `POLICY_AUD` 或 Cloudflare API token 到公開網站。GitHub Pages 工作流程會把這兩個公開值加入靜態建置。更新 `main` 後檢查 Actions 與 Pages；先用測試環境的獨立 D1/Worker 完整走一遍，再清掉測試資料、確認正式庫「尚無資料」，才提供研究入口給受測者。
7. 開啟管理頁，重新整理統計、匯出 CSV、按日期或全部刪除。CSV 只有匿名欄位與題目選項 ID；匯出檔案仍請依團隊權限及保存期限管理。若正式環境需要重置，先於後台刪除，確認主資料為零；Cloudflare D1 回復歷史另依方案保留。

需要自行設定：Cloudflare 帳號與 Zero Trust、正式 D1 ID、Worker URL、管理者登入電郵及 Access policy、TEAM_DOMAIN、POLICY_AUD、保存天數、GitHub Actions 的公開 URL/天數變數。現階段沒有這些帳號設定，**研究後端及管理頁尚未部署上線**。
