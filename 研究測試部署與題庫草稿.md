# 同一題庫研究測試：題目、統計與部署

所有同意者作答原網站同一套八題。資料服務啟用時，入口先問是否同意將匿名結果用於數據分析；不同意仍可照常體驗，作答不送後台。同意者做完八題與「回到自己」後，網站自動送出作答，成功後顯示匿名完成編號。**單次作答無法衡量學習前後的進步，也不能宣稱原提案的提升 20 個百分點目標已達成。**此版未收集行動意願、協助程度與易用性問卷，相關目標無法驗證。

## 八題與計分

題幹、選項、答案與解說集中在 [data/questions.json](data/questions.json)。修改題目時同步更新 `research/core.ts` 的 `QUIZ_VERSION`；資料庫保存作答時的版本及逐題判定。正式測試前請團隊審核題目與告知文字。

| 題目 | 測量目的 | 正確選項 ID |
| --- | --- | --- |
| q1 運動紀錄 | 位置與固定時間線索 | `time`, `route`, `location` |
| q2 公開文件 | 身分、學校與文件附帶線索 | `name`, `email`, `school`, `qr`, `metadata` |
| q3 帳號關聯 | 跨平台身分關聯 | `same` |
| q4 跨平台拼圖 | 身分、位置、時間的綜合判斷 | `identity`, `school`, `location`, `time` |
| q5 運動平台設定 | 路線資料防護 | `privacy` |
| q6 文件公開方式 | 分享前移除身分線索 | `public` |
| q7 帳號使用方式 | 降低跨平台串聯 | `separate` |
| q8 陌生人視角檢查 | 回查修正版仍能確認的資訊 | `none` |

辨識 q1–q4 合計 40 分、防護 q5–q7 合計 40 分、回查 q8 為 20 分。各階段依答對題數等比例計算、四捨五入後相加。複選必須完整答對才算對。後端獨立驗證八題答案及計分。自我檢核只顯示個人建議，不送後台。這套題目會顯示答案及解說，統計只代表本次作答表現。

## 統計與資料

同意並開始時，後端建立內部隨機匿名 ID 及續填憑證；**受測者看到的完成編號只在送出成功後產生**。同一筆重送仍回傳原編號。刪除資料後編號可能跳號。後台顯示同意開始、完成、流失的人數／分母／百分比，完成者平均分數、分數分布、三階段及逐題答對率、錯誤選項、完成時間中位數。沒有資料顯示「尚無資料」。未完成者只算流失，不進分數統計。CSV 每列對應一名同意者，未完成者分數空白。

GitHub Pages 提供靜態網站；Cloudflare Worker 接收匿名作答，D1 保存紀錄。瀏覽器沒有資料庫權限或管理金鑰。Cloudflare Access 保護 `/admin` 與 `/api/admin/*`，Worker 另驗 Access JWT 簽章、發行者、對象與管理者電郵。只有管理者可看統計、下載 CSV、刪除資料。團隊不主動保存姓名、學號、真實帳號、IP、精確位置或裝置 ID；Cloudflare 仍可能依其政策處理請求。瀏覽器同意後於本機暫存進度及續填憑證；清除資料或換裝置會失去續填能力。

`RETENTION_DAYS` 控制主資料保存期限，每日排程清除；後台亦可依開始日期或全部刪除。D1 回復歷史由 Cloudflare 另行管理，主資料刪除不表示回復歷史立即消失。未成年人測試前須依學校規定完成告知與同意。

## 本機測試

1. 安裝 Node.js 24，執行 `npm ci`、`npm test`、`npx tsc --noEmit`、`npm run build`。
2. 執行 `npm run research:db:local` 初始化**獨立本機假資料庫**，再執行 `npm run research:dev`，預設 API 為 `http://127.0.0.1:8788`。
3. 另一個 PowerShell 終端設定 `$env:NEXT_PUBLIC_RESEARCH_API_URL='http://127.0.0.1:8788'`，再執行 `npm run dev`，開啟 `http://localhost:3000/`。未設定此公開 API URL 時，網站維持一般體驗，不出現收資料的同意窗。
4. 本機管理頁未設定正式 Access，應回應 403。自動測試以簽章模擬管理登入、CSV 及刪除；本機假資料不可放進正式 D1。

## 正式部署

1. 團隊確認八題、同意文字、未成年人程序及保存天數。準備 Cloudflare 帳號、D1 和 Zero Trust Access。此版應建立**新的 D1 資料庫**，不要沿用舊 A/B 資料庫。
2. 在自己的電腦執行 `npx wrangler login` 與 `npx wrangler d1 create footprint-single`。將 `research/wrangler.example.jsonc` 複製成 Git 忽略的 `research/wrangler.jsonc`，填入新 D1 ID、`TEAM_DOMAIN`、`POLICY_AUD`、指定管理者 `ADMIN_EMAILS` 及 `RETENTION_DAYS`。`ALLOWED_ORIGIN` 保持 `https://11430106-cloud.github.io`。
3. 執行 `npx wrangler d1 migrations apply footprint-single --config research/wrangler.jsonc --remote`，確認目標是新資料庫，再執行 `npx wrangler deploy --config research/wrangler.jsonc`。
4. 在 Cloudflare Zero Trust → Access → Applications 建立 self-hosted 應用，保護 Worker 的 `/admin` 與 `/api/admin/*`，只允許團隊指定電郵。`/api/start`、`/api/session`、`/api/complete` 供受測網站使用。確認未登入者讀取後台及 CSV 均回應 403。
5. 在 GitHub 儲存庫 Settings → Secrets and variables → Actions → Variables，設定公開的 `RESEARCH_API_URL` 為 Worker URL、`RESEARCH_RETENTION_DAYS` 為相同天數。GitHub Pages 重新建置後，每名訪客都會先看到同意選擇。管理金鑰、Access 設定和 Cloudflare API token 不可放進公開程式碼或對話。
6. 先以獨立測試 Worker/D1 驗證同意、拒絕、完成、重送、退出、CSV 和刪除。正式收案前確認正式後台顯示「尚無資料」。管理者可由後台重新整理、匯出及刪除。

目前沒有團隊 Cloudflare 帳號設定與金鑰，**資料服務與後台尚未上線**。只有設定 `RESEARCH_API_URL` 並重新部署後，正式網站才會顯示同意選擇及連接資料服務。
